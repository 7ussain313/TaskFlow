// Prisma schema — see SYSTEM_DESIGN.md for the domain model this mirrors.
// Models are added in Phase 2; Phase 1 only wires up the datasource/client.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
